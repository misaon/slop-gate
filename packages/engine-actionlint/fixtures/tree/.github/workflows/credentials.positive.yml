on: push
jobs:
  a:
    runs-on: ubuntu-latest
    services:
      db:
        image: private-registry.example/db
        credentials:
          username: ci
          password: hunter2 # HIT
    steps:
      - run: echo hi
