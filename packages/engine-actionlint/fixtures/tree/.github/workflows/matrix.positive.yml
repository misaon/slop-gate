on: push
jobs:
  a:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        exclude:
          - os: solaris-latest # HIT
    steps:
      - run: echo hi
