---
'@misaon/slop-gate': patch
---

Development on pnpm 11.20.0, downloaded from the `packageManager` field rather than taken from PATH.
Nothing about the published packages changes; `engines.node` is unaffected.
