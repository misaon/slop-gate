# Two source files reached `main` with a stray NUL byte where a space belonged (a `Map` key separator
# in a template literal), which makes git classify them as binary and print "Binary files differ"
# instead of a reviewable diff. `diff` forces the textual comparison regardless of byte values; the
# two occurrences this branch touched are also fixed at the source.
*.ts diff
*.tsx diff
